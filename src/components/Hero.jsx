import React from "react";

import ReactCanvas from "./canvas/ReactEl";

const Hero = () => {
  return (
    <div className="h-[500px]">
      <ReactCanvas />
    </div>
  );
};

export default Hero;
